'use strict';

describe('ShoppingCart Detail Controller', function() {
    var $scope, $rootScope;
    var MockEntity, MockShoppingCart, MockUser;
    var createController;

    beforeEach(inject(function($injector) {
        $rootScope = $injector.get('$rootScope');
        $scope = $rootScope.$new();
        MockEntity = jasmine.createSpy('MockEntity');
        MockShoppingCart = jasmine.createSpy('MockShoppingCart');
        MockUser = jasmine.createSpy('MockUser');
        

        var locals = {
            '$scope': $scope,
            '$rootScope': $rootScope,
            'entity': MockEntity ,
            'ShoppingCart': MockShoppingCart,
            'User': MockUser
        };
        createController = function() {
            $injector.get('$controller')("ShoppingCartDetailController", locals);
        };
    }));


    describe('Root Scope Listening', function() {
        it('Unregisters root scope listener upon scope destruction', function() {
            var eventType = 'jewelryApp:shoppingCartUpdate';

            createController();
            expect($rootScope.$$listenerCount[eventType]).toEqual(1);

            $scope.$destroy();
            expect($rootScope.$$listenerCount[eventType]).toBeUndefined();
        });
    });
});
